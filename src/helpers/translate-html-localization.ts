function translateHtmlLocalization(localization: string) {
  // return localization.split(" ").map(word => {
  //   if (word.includes("<") && word.includes(">")) {
  //     return <></>
  //   } else {
  //     return
  //   }
  // })
}

export default translateHtmlLocalization;
